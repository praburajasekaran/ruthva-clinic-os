# TO-DOS

## Change Terminology for Ayurveda Consultation Form - 2026-02-28 00:05

- **Update consultation form terminology for Ayurveda version** - Change labels/terminology across the consultation form to use proper Ayurvedic/Siddha terms. **Problem:** The current consultation form uses a mix of modern medical and traditional terminology that needs to be updated for the Ayurveda version of the app. This includes vitals (Weight, Height, Pulse Rate, Temperature, BP), General Assessment section (Appetite/பசி, Bowel/மலம், Micturition/சிறுநீர், Sleep/தூக்கம்), Envagai Thervu/எண்வகைத் தேர்வு (8-fold examination: Naa/நா, Niram/நிறம், Mozhi/மொழி, Vizhi/விழி, Nadi/நாடி, Mei/மெய், Muthiram/மூத்திரம், Varmam/வர்மம்), Mental State/Attitude, and Diagnosis section (Chief Complaints, History of Present Illness, ICD Code). **Files:** `frontend/src/components/consultations/ConsultationForm.tsx`, `frontend/src/components/consultations/EnvagaiThervu.tsx`, `frontend/src/lib/constants/bilingual-labels.ts`, `frontend/src/lib/constants/envagai-options.ts`, `frontend/src/lib/types.ts`. **Solution:** Review each section's terminology and update labels to use appropriate Ayurvedic equivalents while keeping the bilingual (Tamil/English) approach. May need to create separate label sets for Ayurveda vs Siddha medicine versions.

## Enhance Clinic Signup Form Fields - 2026-02-28 00:06

- **Add doctor and clinic details to signup form** - Expand the clinic signup form to capture salutation (Dr), qualification, doctor registration number, clinic address, and clinic logo. **Problem:** The current signup form only collects clinic name, email, password, and subdomain. It's missing critical professional details: doctor salutation (e.g., Dr.), qualifications (e.g., BSMS, MD), doctor registration number (medical council), clinic address, and clinic logo upload. These are needed for prescriptions, letterheads, and professional identity. **Files:** `frontend/src/app/signup/page.tsx:24-93` (form state and validation), `frontend/src/lib/types.ts:20-41` (Clinic/tenant type definitions — already has `address` and `logo_url` fields in type but not in signup form), `frontend/src/lib/api.ts` (API calls for signup). **Solution:** Add form fields for salutation (dropdown: Dr, Prof, etc.), qualification (text input), registration number (text input), clinic address (textarea), and logo (file upload with preview). Update the Clinic type if needed to include `doctor_salutation`, `doctor_qualification`, and `registration_number`. Backend schema may also need migration.

## Add Medicine / Pharmacy Management - 2026-02-28 00:07

- **Build medicine master database and pharmacy module** - Add a full medicine/pharmacy management feature to the clinic platform. **Problem:** Currently medicines are only free-text entries in prescriptions (`drug_name` field in `Medication` model). There is no master medicine database, no inventory/stock tracking, no pricing, and no medicine search/autocomplete when writing prescriptions. Doctors have to type medicine names manually each time, leading to inconsistencies and no reuse. **Files:** `backend/prescriptions/models.py` (existing `Medication` model with `drug_name`, `dosage`, `frequency`, `duration`, `instructions`), `frontend/src/components/prescriptions/PrescriptionBuilder.tsx` (Rx form), `frontend/src/components/prescriptions/MedicationRow.tsx` (medication entry row), `frontend/src/lib/types.ts` (type definitions), `frontend/src/lib/api.ts` (API client), `frontend/src/components/layout/Sidebar.tsx` (navigation — needs pharmacy link), `backend/config/urls.py` (API routes). **Solution:** Create a new `pharmacy` Django app with a `Medicine` model (name, name_ta, category, form/type like tablet/syrup/powder/choornam/lehyam, manufacturer, unit, price, stock_quantity, reorder_level, expiry tracking). Add CRUD API endpoints and a frontend pharmacy page with medicine list, search, add/edit. Integrate medicine autocomplete into the prescription builder so doctors can search and select from the master list. Consider Siddha/Ayurveda-specific medicine categories (Choornam, Lehyam, Nei, Thailam, Mathirai, Kashayam, etc.).

## Autosuggest Drug Instructions from History - 2026-02-28 00:10

- **Add autofill/autosuggest for drug instructions in prescriptions** - When a doctor selects or types a drug name, auto-populate dosage, frequency, duration, and instructions based on how they previously prescribed that same drug. **Problem:** Doctors repeatedly prescribe the same medicines with the same instructions (e.g., Nilavembu Kashayam is almost always "30ml BD before food"). Currently they must manually type all fields every time, which is slow and error-prone. There's no learning from historical prescription data. **Files:** `backend/prescriptions/models.py` (existing `Medication` model — source of historical prescription data), `frontend/src/components/prescriptions/MedicationRow.tsx` (medication entry row — needs autosuggest UI), `frontend/src/components/prescriptions/PrescriptionBuilder.tsx` (parent Rx form), `frontend/src/lib/api.ts` (API client — needs new endpoint), `backend/config/urls.py` (API routes). **Solution:** Add a backend API endpoint that, given a drug name (and optionally the clinic/doctor context), returns the most frequently used dosage, frequency, duration, and instructions for that drug from past prescriptions. On the frontend, when a drug is selected/typed in `MedicationRow`, call this endpoint and auto-populate the remaining fields as suggestions the doctor can accept or override. Could use a simple frequency-based approach (most common values) or show a dropdown of recent instruction patterns for that drug.

## Add User Profile Picture Upload - 2026-02-28 00:10

- **Enable profile picture upload for users** - Allow doctors/therapists/admins to upload and manage their own profile picture. **Problem:** The `User` model has no profile picture field at all — no `avatar`, `profile_pic`, or `photo` field exists. Users have no way to personalize their account with a photo, which is useful for the UI header, prescription letterheads, and multi-doctor clinics where visual identification helps. **Files:** `backend/users/models.py:5-27` (User model — needs `profile_pic` ImageField or URL field), `frontend/src/lib/types.ts` (User type definition — needs profile pic property), `frontend/src/lib/api.ts` (API client — needs upload endpoint), `frontend/src/components/layout/Sidebar.tsx` (could show user avatar), `frontend/src/app/(dashboard)/` (needs a settings/profile page or section for upload). **Solution:** Add a `profile_pic` field to the User model (either `ImageField` with media storage or a URL field for external storage like S3/Cloudinary). Add the upload UI to the settings page where users can upload/change their photo with preview and crop. Add a backend API endpoint for multipart file upload. Display the avatar in the sidebar header and anywhere the user identity is shown. Consider falling back to initials-based avatar when no photo is set.

## Free Plan Team and Patient Limits - 2026-02-28 12:23

- **Enforce free plan team limits (1 doctor, 1 therapist, 1 admin)** - Limit the free plan to a maximum of 3 team members: one doctor (who is the primary contact/clinic owner), one therapist, and one admin staff. **Problem:** Currently there are no plan-based restrictions on team size. Any clinic owner can invite unlimited members regardless of their subscription tier. The free plan should cap the team at 3 members with specific role limits: 1 doctor (primary contact), 1 therapist, 1 admin. **Files:** `backend/clinics/views.py:60-81` (team_invite view — needs plan-aware validation before creating invitation), `backend/clinics/serializers.py` (InviteMemberSerializer — could add plan limit validation), `backend/clinics/models.py` (Clinic model — needs a `plan` field or FK to a Plan model), `frontend/src/app/(dashboard)/team/page.tsx:67-72` (Invite button — should show upgrade prompt when limit reached). **Solution:** Add a `plan` field to the Clinic model (or a separate Plan/Subscription model) with a `free` tier. In the invite flow, check current team composition against plan limits before allowing new invitations. Show a clear message in the UI when limits are reached (e.g., "Upgrade to invite more team members"). Enforce both per-role limits (max 1 of each role on free) and total team size (max 3 on free).

- **Enforce free plan patient limit (200 patients)** - Cap patient registration at 200 for clinics on the free plan. **Problem:** No limit exists on the number of patients a clinic can register. The free plan should allow management of up to 200 patients. Beyond that, the clinic should be prompted to upgrade. **Files:** `backend/patients/views.py` (patient creation view — needs plan-aware count check), `backend/patients/models.py` (Patient model — linked to clinic), `backend/clinics/models.py` (Clinic model — needs plan field), `frontend/src/app/(dashboard)/patients/page.tsx` (patient list — should show count vs limit, upgrade prompt). **Solution:** Before creating a new patient, check `Patient.objects.filter(clinic=request.clinic).count()` against the plan's patient limit (200 for free). Return a clear error if limit reached. Show remaining capacity in the frontend patients list (e.g., "180/200 patients"). Add an upgrade CTA when approaching or at the limit.

## Direct Login for Client Clinics Without OTP - 2026-04-03 19:52

- **Add password-based or token login bypass for client clinic dashboards** - Allow clinic owners/staff to log in without OTP for local dev and client demos. **Problem:** The current login flow requires OTP email delivery (`backend/users/views.py:48` `request_otp`), which fails locally when no email service is configured ("Failed to send code" error). Demo accounts work with hardcoded code `123456`, but real client clinics have no bypass. This blocks local testing and client onboarding demos. **Files:** `backend/users/views.py:48-62` (request_otp view — demo check returns `123456`), `backend/users/demo.py:34-40` (is_demo_user check), `frontend/src/app/login/page.tsx:28-43` (handleRequestOTP — shows error on failure). **Solution:** Options: (1) Add a `DEV_OTP_BYPASS=true` env flag that auto-approves any OTP as `123456` in non-production, (2) Add password-based login as an alternative to OTP (username+password already supported via `CustomTokenObtainPairSerializer`), (3) Add a management command to generate a login token/URL for a specific clinic user. The JWT password login path already exists at `/auth/token/` — may just need a UI toggle on the login page to switch between OTP and password login.
